@model FoodFit.Models.RationWorkout

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>RationWorkout</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RationID" class="control-label"></label>
                <select asp-for="RationID" class ="form-control" asp-items="ViewBag.RationID"></select>
            </div>
            <div class="form-group">
                <label asp-for="WorkoutID" class="control-label"></label>
                <select asp-for="WorkoutID" class ="form-control" asp-items="ViewBag.WorkoutID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
